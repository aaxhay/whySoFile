<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhysoFile</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <!-- <div class="titleDiv py-7 flex justify-center item-center">
            <h1 class="text-5xl  text-white"><i>Excuses.</i></h1>
        </div> -->
        <div class="form p-10">
            <form id="myForm" action="/create" method="post">
                <input class="w-1/4 px-5 py-3 text-lg block font-md bg-zinc-800 outline-none text-white rounded-md" required type="text" placeholder="Your Title Goes Here" name="Title">
                <textarea class="w-3/4 px-5 py-3 text-clip text-lg block font-md bg-zinc-800 resize-none mt-3 outline-none text-white rounded-md" required placeholder="Your Description Goes Here" name="Description"></textarea>
                <input class="px-5 py-2 rounded-lg bg-blue-500 font-bold text-white mt-3" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="w-72 px-5 py-2 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>
                       <div class="w-full flex justify-between item-center mt-3">
                        <a class="text-blue-600" href="/file/<%= val%>">ReadMore.</a>
                        <a class="text-green-500" href="/edit/<%=val%>">Edit Filename.</a>
                       </div>
                       <div class="delete-div mt-2">
                          <a href="/delete/<%= val%>"><img src="/images/delete (2).png" alt="" height="50" ></a>
                       </div>
                    </div>
                <% })%>
           <% } else { %>
            <h1 class="capitalize text-zinc-500 text-2xl">Nothing to show right now.</h1>
            <% } %>
        </div>
    </div>
    <script src="/javascripts/script.js"></script>
</body>
</html>